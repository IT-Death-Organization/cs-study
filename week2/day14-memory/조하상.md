# Day 14: 메모리 관리 (페이징, 세그먼테이션)
> 📅 2025.02.26 | 📁 Week 2

---

## 🧠 학습 질문
- [x] 메모리 단편화는 왜 발생하는가?
- [x] 내부 단편화와 외부 단편화의 차이는? 각각 어떤 경우에 발생하는가?
- [x] 페이징이란 무엇이며 왜 사용하는가? 단편화 문제를 어떻게 해결하는가?
- [x] 페이지 테이블은 어떻게 동작하는가? 주소 변환 과정은?
- [x] 세그먼테이션과 페이징의 차이는? 각각의 장단점은?
- [x] 페이지 크기가 성능에 미치는 영향은? 크면/작으면 어떤 문제가 있는가?

---
## 1. 메모리 단편화는 왜 발생하는가?

**비연속적인 메모리 할당과 해제**
- 프로세스들이 메모리에 적재되고 제거되는 과정이 반복되면서, 프로세스가 점유했던 메모리 공간이 조각조각 나뉘기 때문이다.
- 운영체제가 메모리를 할당할 때 프로세스의 크기에 딱 맞는 연속된 공간을 찾지 못하게 되면, 전체 자유 공간은 충분함에도 불구하고 새로운 프로세스를 실행할 수 없는 비효율이 발생한다.

---
## 2. 내부 단편화와 외부 단편화의 차이는?
- **내부 단편화 (Internal Fragmentation):** 프로세스가 요청한 크기보다 더 큰 메모리 블록이 할당되어, 해당 블록 내부에서 남는 공간이 발생하는 현상이다. 고정 분할 방식에서 주로 발생한다.
- **외부 단편화 (External Fragmentation):** 메모리 상의 빈 공간들의 총합은 프로세스를 수용하기에 충분하지만, 공간들이 연속적이지 않고 파편화되어 있어 프로세스를 할당하지 못하는 현상이다. 가변 분할 방식에서 주로 발생한다.

---
## 3. 페이징이란 무엇이며 왜 사용하는가?

**고정 크기 분할을 통한 해결**
- 물리 메모리를 **프레임(Frame)**이라는 고정된 크기로 나누고, 프로세스의 논리 메모리를 **페이지(Page)**라는 동일한 크기의 블록으로 나누어 관리하는 기법이다.
- **사용 이유:** 프로세스의 페이지들을 물리 메모리의 불연속적인 프레임에 나누어 배치할 수 있기 때문에 **외부 단편화 문제를 근본적으로 해결**할 수 있다. 또한 메모리 압축(Compaction)과 같은 비용이 큰 작업이 필요 없어진다.

---
## 4. 페이지 테이블은 어떻게 동작하는가? (주소 변환 과정)

**논리 주소에서 물리 주소로의 매핑**
- CPU가 생성하는 논리 주소는 **페이지 번호(p)**와 **변위(d, offset)**로 구성된다.
- **동작 과정:**
  1. 페이지 번호(p)를 이용해 메모리에 저장된 **페이지 테이블(Page Table)**을 조회한다.
  2. 테이블에서 해당 페이지가 위치한 물리 메모리의 **프레임 번호(f)**를 찾아낸다.
  3. 찾아낸 프레임 번호(f)와 기존의 변위(d)를 결합하여 실제 물리 주소를 생성한다.
  4. 이를 통해 CPU는 실제 데이터가 위치한 메모리 주소에 접근한다.

---
## 5. 세그먼테이션과 페이징의 차이는?

- **페이징 (Paging):** 메모리를 물리적인 고정 크기로 나눈다. 외부 단편화는 없지만 내부 단편화가 발생할 수 있다. 하드웨어 관점에서 구현이 비교적 쉽다.
- **세그먼테이션 (Segmentation):** 메모리를 논리적 의미 단위(코드, 데이터, 스택 등)인 세그먼트로 나눈다. 크기가 가변적이므로 내부 단편화는 없지만 **외부 단편화**가 발생할 수 있다. 공유와 보호 관점에서 유리하다.

---
## 6. 페이지 크기가 성능에 미치는 영향은?

**페이지가 작을 때**
- **장점:** 내부 단편화가 줄어들어 메모리를 더 정밀하게 사용할 수 있다.
- **단점:** 페이지 테이블의 행(Entry) 개수가 많아져 테이블 관리에 필요한 메모리 오버헤드가 커지며, TLB(캐시) 히트율이 낮아질 수 있다.

**페이지가 클 때**
- **장점:** 페이지 테이블 크기가 줄어들고, 디스크 I/O 시 한 번에 많은 양을 가져오므로 효율적이다. TLB 히트율도 높아진다.
- **단점:** 프로세스 마지막 페이지에서 낭비되는 공간이 커져 **내부 단편화** 문제가 심화된다.

---
## 📎 참고 자료
<!-- 공부하면서 참고한 링크를 여기에 추가해주세요 -->

---

## 💬 토론 포인트
<!-- PR 리뷰 또는 스터디 중 나온 추가 질문이나 논의 사항을 기록해주세요 -->
