# Day 9: 연결 리스트(Linked List)
> 📅 2025.02.21 | 📁 Week 2
---
## 🧠 학습 질문
- [x] 연결 리스트의 중간 삽입이 O(1)이라는 말의 전제 조건은?
- [x] 실제로 중간 위치를 찾으려면 O(n)이 걸리는데 왜 O(1)이라고 하는가?
- [x] 이중 연결 리스트가 단일 연결 리스트보다 유리한 점과 단점은?
- [x] 연결 리스트에서 사이클을 탐지하는 방법은? (Floyd's Cycle Detection)
- [x] Runner 기법(Fast & Slow Pointer)은 어떻게 동작하는가?
- [x] 연결 리스트를 뒤집는(reverse) 알고리즘을 설명하면?
---
## 1. 연결 리스트의 중간 삽입이 O(1)이라는 말의 전제 조건은?

**참조값의 사전 확보**
- 삽입하려는 위치의 **직전 노드(Predecessor Node)에 대한 포인터(주소값)를 이미 알고 있어야 한다.**
- 연결 리스트의 삽입은 이전 노드의 `next`를 새 노드로 연결하고, 새 노드의 `next`를 기존의 다음 노드로 연결하는 과정만 포함하기 때문이다.

---
## 2. 실제로 중간 위치를 찾으려면 O(n)이 걸리는데 왜 O(1)이라고 하는가?

**탐색과 삽입 연산의 분리**
- 알고리즘의 시간 복잡도를 정의할 때, 특정 위치를 찾아가는 **'탐색(Search)'**과 실제 데이터를 넣는 **'삽입(Insertion)'** 동작을 독립적으로 계산한다.
- 배열(Array)은 특정 인덱스에 접근하는 것은 $O(1)$이지만, 삽입 시 데이터를 뒤로 밀어내는 'Shift' 과정 때문에 삽입 연산 자체가 $O(n)$이다. 
- 반면 연결 리스트는 탐색에 $O(n)$이 걸릴지라도, 일단 위치를 찾은 후 행하는 **데이터 삽입 행위 자체는 포인터 두 개만 바꾸면 끝나는 $O(1)$ 작업**이므로 물리적인 효율성이 높다고 판단한다.

---

## 3. 이중 연결 리스트가 단일 연결 리스트보다 유리한 점과 단점은?

**유리한 점 (Pros)**
- **양방향 탐색:** 각 노드가 `prev`와 `next` 포인터를 모두 가지므로 앞뒤 양방향으로 자유롭게 이동할 수 있다.
- **삭제의 용이성:** 특정 노드를 삭제할 때, 단일 연결 리스트와 달리 별도의 탐색 없이 `prev` 포인터를 이용해 이전 노드에 즉시 접근하여 연결을 수정할 수 있다.

**단점 (Cons)**
- **메모리 오버헤드:** 노드마다 이전 노드의 주소를 저장할 공간이 추가로 필요하므로 메모리 사용량이 늘어난다.
- **관리 복잡도:** 삽입이나 삭제 시 수정해야 할 포인터가 단일 연결 리스트에 비해 두 배로 많아져 구현이 복잡해진다.

---

## 4. 연결 리스트에서 사이클을 탐지하는 방법은? (Floyd's Cycle Detection)

**플로이드의 순환 탐지 알고리즘**
- 두 개의 포인터를 서로 다른 속도로 이동시켜 리스트 내부의 순환(Cycle) 여부를 확인한다.
- 한 지점에서 출발한 두 포인터가 리스트를 순회하다가 **어느 시점에 동일한 노드에서 만나게 된다면**, 해당 리스트에는 사이클이 존재한다고 판단한다. 이는 마치 트랙 위에서 속도가 빠른 주자가 느린 주자를 한 바퀴 추월하여 만나는 원리와 같다.

---

## 5. Runner 기법(Fast & Slow Pointer)은 어떻게 동작하는가?

**동작 방식**
- **Slow Pointer:** 한 번의 스텝에 노드 한 칸씩 이동한다.
- **Fast Pointer:** 한 번의 스텝에 노드 두 칸씩 이동한다.
- 두 포인터의 속도 차이를 이용해 특정 지점을 찾아낸다. 예를 들어, `Fast Pointer`가 리스트의 끝(`null`)에 도달했을 때 `Slow Pointer`는 정확히 리스트의 **중간 지점**에 위치하게 된다.

---

## 6. 연결 리스트를 뒤집는(reverse) 알고리즘을 설명하면?

**포인터 방향 재설정**
- 리스트를 순회하며 각 노드의 `next` 포인터가 다음 노드가 아닌 **이전 노드를 가리키도록** 방향을 바꾼다.
- 이를 위해 세 개의 포인터(`prev`, `curr`, `next`)를 사용한다.

**실행 순서**
1. 현재 노드의 다음 위치를 `next`에 임시 저장한다.
2. 현재 노드의 연결 방향을 뒤로 돌린다 (`curr.next = prev`).
3. `prev`를 현재 노드로, `curr`를 아까 저장해둔 `next`로 한 칸씩 이동시킨다.
4. `curr`이 `null`이 될 때까지 반복하며, 마지막에 남은 `prev`가 뒤집힌 리스트의 새로운 헤드가 된다.
---
## 📎 참고 자료
<!-- 공부하면서 참고한 링크를 여기에 추가해주세요 -->
---
## 💬 토론 포인트
<!-- PR 리뷰 또는 스터디 중 나온 추가 질문이나 논의 사항을 기록해주세요 -->
