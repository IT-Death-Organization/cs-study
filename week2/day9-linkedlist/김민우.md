# Day 9: 연결 리스트(Linked List)
> 📅 2025.02.21 | 📁 Week 2
---
## 🧠 학습 질문
- [x] 연결 리스트의 중간 삽입이 O(1)이라는 말의 전제 조건은?
- [x] 실제로 중간 위치를 찾으려면 O(n)이 걸리는데 왜 O(1)이라고 하는가?
- [x] 이중 연결 리스트가 단일 연결 리스트보다 유리한 점과 단점은?
- [x] 연결 리스트에서 사이클을 탐지하는 방법은? (Floyd's Cycle Detection)
- [x] Runner 기법(Fast & Slow Pointer)은 어떻게 동작하는가?
- [x] 연결 리스트를 뒤집는(reverse) 알고리즘을 설명하면?
---

# 연결 리스트 핵심 개념 정리

---

## 1. 연결 리스트의 "중간 삽입이 O(1)"이라는 말의 전제 조건

### 전제 조건

삽입 위치의 노드를 **이미 알고 있을 때**를 의미한다.

- `prev` 노드를 이미 참조하고 있음
- 또는 삽입 위치의 노드 포인터를 직접 가지고 있음

그 상태에서 수행하는 작업은 포인터 변경 2번에 불과하다.

```
new.next = prev.next
prev.next = new
```

포인터 변경 2번 → 상수 시간 → O(1)

### 왜 실제로는 O(n)처럼 느껴지나?

중간 위치를 찾기 위해 순차 탐색이 필요하기 때문이다.

```
head → head.next → head.next.next ...
```

### 정리

| 단계 | 시간 복잡도 |
|------|------------|
| 위치 탐색 | O(n) |
| 노드 삽입 | O(1) |

교과서에서 말하는 "중간 삽입 O(1)"은 탐색 비용을 제외한 순수 삽입 연산만을 의미한다.

---

## 2. 이중 연결 리스트 vs 단일 연결 리스트

### 단일 연결 리스트 (Singly Linked List)

**구조**

```
[data | next]
```

**장점**
- 메모리 절약 (next 포인터 1개)
- 구조 단순

**단점**
- 이전 노드로 이동 불가
- 삭제 시 이전 노드 필요
- 역방향 탐색 불가

---

### 이중 연결 리스트 (Doubly Linked List)

**구조**

```
[prev | data | next]
```

**장점**

양방향 탐색이 가능하며, 현재 노드만 알아도 삭제할 수 있다.

```
node.prev.next = node.next
node.next.prev = node.prev
```

삭제 연산이 O(1)이고, LRU Cache 구현에 적합하다.

**단점**
- 메모리 2배 포인터 사용
- 구현 복잡
- 포인터 갱신 실수 위험 증가

---

## 3. 연결 리스트에서 사이클 탐지 (Floyd's Cycle Detection)

### 핵심 아이디어

두 포인터를 사용한다.

- `slow` — 1칸씩 이동
- `fast` — 2칸씩 이동

사이클이 있으면 두 포인터는 결국 만난다.

### 동작 원리

- 사이클이 없으면: `fast`가 `null`에 도달하여 종료
- 사이클이 있으면: `fast`가 `slow`를 따라잡아 내부에서 반드시 만남

### 왜 반드시 만나는가?

사이클 내부에서 `slow`의 속도는 1, `fast`의 속도는 2이므로 속도 차이가 1이다. 원형 트랙에서 두 포인터의 거리 차이가 사이클 길이의 배수가 되는 순간 만나게 된다.

---

## 4. Runner 기법 (Fast & Slow Pointer)

서로 다른 속도로 이동하는 두 포인터를 사용하는 기법이다.

**활용 예시**

- 사이클 탐지
- 중간 노드 찾기

**중간 노드 찾기 예시**

```java
while (fast != null && fast.next != null) {
    slow = slow.next;
    fast = fast.next.next;
}
// 종료 시 slow는 중간 위치
```

---

## 5. 연결 리스트 뒤집기 (Reverse)

### 핵심 원리

포인터 방향을 하나씩 반대로 바꾼다.

### 알고리즘 (Iterative)

```java
Node prev = null;
Node curr = head;

while (curr != null) {
    Node next = curr.next;
    curr.next = prev;
    prev = curr;
    curr = next;
}

head = prev;
```

매 반복마다 `curr.next` 방향을 뒤로 바꾸는 것이 핵심이다.

- 시간 복잡도: O(n)
- 공간 복잡도: O(1)

## 📎 참고 자료
<!-- 공부하면서 참고한 링크를 여기에 추가해주세요 -->
---
## 💬 토론 포인트
<!-- PR 리뷰 또는 스터디 중 나온 추가 질문이나 논의 사항을 기록해주세요 -->
