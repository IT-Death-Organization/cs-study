# 연결 리스트(Linked List)

> 📅 2025.02.21 | 📁 Week 2
> 

---

## 🧠 학습 질문

- [x]  연결 리스트의 중간 삽입이 O(1)이라는 말의 전제 조건은?
- [x]  실제로 중간 위치를 찾으려면 O(n)이 걸리는데 왜 O(1)이라고 하는가?
- [x]  이중 연결 리스트가 단일 연결 리스트보다 유리한 점과 단점은?
- [x]  연결 리스트에서 사이클을 탐지하는 방법은? (Floyd's Cycle Detection)
- [x]  Runner 기법(Fast & Slow Pointer)은 어떻게 동작하는가?
- [x]  연결 리스트를 뒤집는(reverse) 알고리즘을 설명하면?

---

### **연결 리스트 중간 삽입 O(1)**

• 전제 조건: 삽입하려는 위치의 노드 포인터(또는 이전 노드의 포인터)를 이미 확보한 상태여야 함.
• 논리적 근거: 연결 리스트는 배열처럼 데이터를 뒤로 밀어내는(Shifting) 과정이 없음. 단순히 새로운 노드의 `next`를 다음 노드에 연결하고, 이전 노드의 `next`를 새 노드로 교체하는 포인터 대입 연산만 수행하면 되기 때문에 연산 횟수가 상수로 고정됨.

### 실제로 중간 위치를 찾으려면 O(n)이 걸리는데 왜 O(1)이라고하는가?

• **$O(n)$ 탐색과의 관계:** "중간 삽입이 $O(n)$이다"라고 말하는 경우는 '특정 값을 가진 노드를 찾아서 그 뒤에 삽입'하는 전체 과정을 의미함. 하지만 자료구조 정의상 **'삽입'이라는 행위 자체**는 포인터 변경만 포함하므로 $O(1)$로 규정함.

 **이중 연결 리스트(Doubly)의 실전적 이해**
단일 연결 리스트의 단방향 제약을 해결하기 위해 `prev` 포인터를 추가한 구조임.
• **유리한 점:**
    ◦ **역방향 탐색:** 뒤에서부터 데이터를 찾아야 하거나, 브라우저의 '뒤로 가기/앞으로 가기' 같은 기능을 구현할 때 필수적임.
    ◦ **삭제의 효율성:** 단일 연결 리스트에서 특정 노드를 삭제하려면 그 **이전 노드**를 알아야 하므로 결국 처음부터 탐색($O(n)$)해야 함. 반면, 이중 연결 리스트는 삭제 대상 노드가 `prev`를 알고 있어 즉시 이전 노드와 다음 노드를 연결할 수 있음($O(1)$).
• **단점:**
    ◦ **메모리 소모:** 각 노드마다 포인터 변수가 하나씩 더 추가되므로 데이터가 많아질수록 메모리 점유율이 눈에 띄게 증가함.
    ◦ **관리 복잡도:** 삽입/삭제 시 앞뒤 포인터를 모두 끊고 다시 이어줘야 하므로 구현 시 실수(포인터 유실)할 가능성이 높음.
****

### **사이클 탐지: Floyd's Cycle Detection**

리스트의 끝이 다시 앞부분의 어느 노드와 연결되어 무한 루프가 발생하는지 확인하는 알고리즘임.
• 동작 원리 (토끼와 거북이):
    ◦ 두 개의 포인터를 첫 번째 노드에서 출발시킴.
    ◦ 거북이(`slow`)는 한 번에 한 칸씩 이동하고, 토끼(`fast`)는 한 번에 두 칸씩 이동함.
• 판단 결과: 리스트 내에 사이클이 있다면, 속도가 빠른 토끼가 사이클을 돌다가 결국 거북이의 뒤를 따라잡아 두 포인터가 만나는 지점이 반드시 발생함. 사이클이 없다면 토끼가 먼저 리스트의 끝(`null`)에 도달함.

### **Runner 기법 (Fast & Slow Pointer)**

단순 순회 이상의 정보를 얻기 위해 두 포인터의 속도 차이나 간격을 활용하는 전략임.
• **활용 1 (중간 노드 찾기):** `fast`가 두 칸 갈 때 `slow`가 한 칸 가면, `fast`가 리스트 끝에 도달하는 순간 `slow`는 정확히 **전체 리스트의 중간**을 가리키게 됨. 리스트의 길이를 미리 모를 때 유용함.
• **활용 2 (뒤에서 N번째 찾기):** 두 포인터의 간격을 $N$만큼 벌려둔 뒤, 두 포인터를 같은 속도로 이동시킴. 앞선 포인터가 끝에 도달하면 뒤따르던 포인터가 뒤에서 $N$번째 노드에 위치함.
****

### **연결 리스트 뒤집기 (Reverse) 알고리즘ㅌㅌ**

메모리 공간을 추가로 쓰지 않고(In-place), 기존 포인터들의 방향만 바꿔서 리스트를 역순으로 만드는 방법임.
• **핵심 로직 (세 포인터의 이동):**
    1. **prev (이전):** 이미 뒤집힌 노드들의 헤드를 가리킴 (초기값 NULL).
    2. **curr (현재):** 현재 방향을 바꿀 대상 노드.
    3. **next (다음):** 현재 노드의 연결을 끊기 전, 원래 다음 노드를 잃어버리지 않게 임시 저장함.
• **수행 단계:** `next = curr.next`로 다음을 보관 → `curr.next = prev`로 링크 방향 역전 → `prev`와 `curr`를 한 칸씩 전진. 이 과정을 리스트 끝까지 반복함

## 📎 참고 자료

## <!-- 공부하면서 참고한 링크를 여기에 추가해주세요 -->

## 💬 토론 포인트

<!-- PR 리뷰 또는 스터디 중 나온 추가 질문이나 논의 사항을 기록해주세요 -->