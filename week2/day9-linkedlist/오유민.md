# Day 9: 연결 리스트(Linked List)

> 📅 2025.02.21 | 📁 Week 2

---

## 🧠 학습 질문

- [ ] 연결 리스트의 중간 삽입이 O(1)이라는 말의 전제 조건은?
- [ ] 실제로 중간 위치를 찾으려면 O(n)이 걸리는데 왜 O(1)이라고 하는가?
- [ ] 이중 연결 리스트가 단일 연결 리스트보다 유리한 점과 단점은?
- [ ] 연결 리스트에서 사이클을 탐지하는 방법은? (Floyd's Cycle Detection)
- [ ] Runner 기법(Fast & Slow Pointer)은 어떻게 동작하는가?
- [ ] 연결 리스트를 뒤집는(reverse) 알고리즘을 설명하면?

---

### 📌 [Q1] 연결 리스트의 중간 삽입이 O(1)이라는 말의 전제 조건은?

**삽입할 위치의 포인터를 이미 알고 있어야 한다.**
연결 리스트의 중간 삽입이 O(1)인 이유는 노드 사이의 연결(Link)만 바꿔주면 되기 때문이다.
하지만 그 위치까지 찾아가는 과정이 추가된다면 시간 복잡도가 O(n)으로 달라질 수 있다. (해당 위치를 찾기 위해 탐색이 필요하므로)

- 포인터를 알고 있는 경우: O(1)
  - 삽입하려는 위치의 바로 앞 노드에 대한 참조값을 이미 가지고 있다면, 새로운 노드를 생성하고 연결하면 된다.
- 포인터를 모르는 경우: O(n)
  - 특정 값을 가진 노드 뒤에 삽입하거나 k번째 위치에 삽입해야 한다면, head부터 해당 위치까지 일일이 찾아가야 하므로 탐색 시간에 의해 O(n)이 된다.

**동작 과정**

1. 새로운 노드(new) 생성
2. new의 다음 노드 가리킴(next)을 앞 노드(Prev)가 가리키던 노드로 설정
3. prev의 다음 노드 가리킴을 new로 설정

### 📌 [Q2] 실제로 중간 위치를 찾으려면 O(n)이 걸리는데 왜 O(1)이라고 하는가?

**탐색(search) 연산과 삽입(insert) 연산을 엄격하게 분리해서 정의**하기 때문이다.
CS에서 시간 복잡도를 계산할 때에는 각 동작을 독립적인 단위로 본다.

- 탐색 = k번째 노드가 어디 있는지 찾는 행위 -> O(n)
- 삽입 = 특정 노드 바로 뒤에 새 노드를 끼워 넣는 행위 -> O(1)

즉, 탐색 비용(O(n))은 별개이고, 순수하게 노드를 끼워 넣는 삽입 자체는 데이터 개수와 상관없이 O(1)로 일정하다는 뜻이다.
삽입 연산 자체는 포인터 변경만 수행하기 때문이다.
다만 실제로 탐색 과정까지 포함하면 O(n)이 된다.

**배열과 비교**

- 배열
  - 탐색: O(1) (index 접근)
  - 삽입: O(n) (shift 필요)

- 연결 리스트
  - 탐색: O(n)
  - 삽입: O(1) (노드 알고 있을 때)

### 📌 [Q3] 이중 연결 리스트가 단일 연결 리스트보다 유리한 점과 단점은?

이중 연결 리스트 (이전 노드와 다음 노드를 모두 가리킴)
`prev | data | next`

**장점**

- 양방향 탐색 가능
  - 뒤에서부터 탐색 가능
  - 역순 순회 가능 (브라우저 뒤로가기)
- 특정 노드 삭제 효율적임 (O(1))
  - 단일 연결 리스트: 특정 노드를 삭제하려면 그 이전 노드의 포인터를 알아야 하지만, 단일 연결 리스트는 이전 노드로 돌아갈 수 없으므로 결국 head부터 해당 노드까지 찾아가야 해 O(n)이 됨
  - 이중 연결 리스트: 삭제하려는 노드가 본인 기준 prev, next 노드 정보를 모두 갖고 있으므로 탐색 과정 없이 즉시 연결을 해제하고 재구성할 수 있어 O(1)

**단점**

- 메모리 오버헤드
  - 이전 주소를 저장하기 위한 포인터 prev가 추가로 필요함
- 구현 더 복잡함
  - 노드 삽입, 삭제 시 관리해야 할 포인터가 두 배로 늘어남 (이전 노드의 prev와 next, 자기 자신의 prev와 next까지 총 4개의 포인터 관리)

### 📌 [Q4] 연결 리스트에서 사이클을 탐지하는 방법은? (Floyd's Cycle Detection)

**플로이드의 순환 탐지 알고리즘**이다. (토끼와 거북이)
이 알고리즘은 서로 다른 속도를 가진 두 개의 포인터를 사용한다.
두 개의 포인터를 서로 다른 속도로 움직이면, 사이클이 있다면 반드시 만나게 된다는 전제이다.

- 거북이 (Slow pointer): 한 번에 한 칸씩 이동한다.
- 토끼 (Fast pointer): 한 번에 두 칸씩 이동한다.

**동작 방식**

1. 두 포인터를 모두 리스트의 시작점인 head에서 출발시킨다.
2. 리스트에 사이클이 없다면, 속도가 빠른 토끼가 먼저 리스트의 끝(null)에 도달하게 된다.
3. 리스트에 사이클이 있다면 토끼는 사이클 내부를 빙글빙글 돌게 된다. 이때 거북이도 결국 사이클에 진입하게 되고, 어느 시점에서 이 두 포인터는 반드시 만나게 된다. (두 포인터 사이의 거리는 매 스텝마다 1씩 줄어들다가 결국 0이 됨. == 두 포인터가 동일한 노드를 가리킴)

### 📌 [Q5] Runner 기법(Fast & Slow Pointer)은 어떻게 동작하는가?

- Slow Runner (거북이): 한 번의 스텝에 한 칸씩 이동 (slow = slow.next)
- Fast Runner (토끼): 한 번의 스텝에 두 칸씩 이동 (fast = fast.next.next)

- 중간 지점 찾기: 토끼가 끝에 도착하면, 거북이는 정확히 중간에 있음 (전체 길이를 몰라도 됨!)
- 사이클 탐지: 리스트에 루프가 있으면, 토끼가 한 바퀴 돌아 결국 거북이를 뒤에서 만남
- 뒤에서 k번째 찾기: 토끼를 k만큼 먼저 보낸 뒤 같이 달리면, 토끼가 끝날 때 거북이는 뒤에서 k번째에 있음

### 📌 [Q6] 연결 리스트를 뒤집는(reverse) 알고리즘을 설명하면?

**각 노드의 next 방향을 반대로 바꾸면 된다.**
연결 리스트를 뒤집을 때는 세 개의 포인터(prev, curr, next)를 사용한다.

prev (이전): 현재 노드의 next가 가리킬 대상 (처음에는 null)

curr (현재): 연결을 뒤집을 대상 노드

next (다음): 연결을 끊기 전, 다음 위치를 저장해두는 용도

현재 노드의 next를 이전 노드를 가리키도록 변경하고, 포인터를 한 칸씩 이동시키는 과정을 반복한다.
한 번의 순회로 가능하므로 시간복잡도는 O(n), 공간복잡도는 O(1)이 된다.

---

## 📎 참고 자료

## <!-- 공부하면서 참고한 링크를 여기에 추가해주세요 -->

## 💬 토론 포인트

<!-- PR 리뷰 또는 스터디 중 나온 추가 질문이나 논의 사항을 기록해주세요 -->
