# Day 9: 연결 리스트(Linked List)
> 📅 2025.02.21 | 📁 Week 2

---

## 🧠 학습 질문
- [x] 연결 리스트의 중간 삽입이 O(1)이라는 말의 전제 조건은?
- [x] 실제로 중간 위치를 찾으려면 O(n)이 걸리는데 왜 O(1)이라고 하는가?
- [x] 이중 연결 리스트가 단일 연결 리스트보다 유리한 점과 단점은?
- [x] 연결 리스트에서 사이클을 탐지하는 방법은? (Floyd's Cycle Detection)
- [x] Runner 기법(Fast & Slow Pointer)은 어떻게 동작하는가?
- [x] 연결 리스트를 뒤집는(reverse) 알고리즘을 설명하면?

---

## Q1. 연결 리스트의 중간 삽입이 O(1)이라는 말의 전제 조건은?

[답변]
삽입 위치를 알고 있어야 한다.
== prev 노드의 참조가 주어진 상태

* 단일 연결 리스트: prev.next = newNode / newNode.next = next
* 이중 연결 리스트: prev.next = newNode / newNode.prev = prev, newNode.next = next / next.prev = newNode

---

## Q2. 실제로 중간 위치를 찾으려면 O(n)이 걸리는데 왜 O(1)이라고 하는가?

[답변]
* 탐색: O(n)
* 삽입: O(1)
삽입 위치를 알고 있다는 전제
위치만 알면 노드 밀기 없이 포인터만 바꾸면 되기 때문

---

## Q3. 이중 연결 리스트가 단일 연결 리스트보다 유리한 점과 단점은?

[답변]
* 유리한 점
  - 양방향 순회가 가능함
  - 양 끝에서의 삽입/삭제가 효율적
* 단점
  - 메모리 오버헤드 증가
  - 포인터 업데이트 증가

---

## Q4. 연결 리스트에서 사이클을 탐지하는 방법은? (Floyd's Cycle Detection)

[답변]
* Floyd's Cycle Detection
  - Floyd의 토끼-거북이 알고리즘(빠른/느린 포인터)
  - 두 포인터: slow, fast
    - slow: 한 번에 1칸(slow = slow.next)
    - fast: 한 번에 2칸(fast = fast.next.next)
    * 순회 중 fast.next = null: 사이클 X
    * 순회 중 slow == fast: 사이클 O

---

## Q5. Runner 기법(Fast & Slow Pointer)은 어떻게 동작하는가?

[답변]
* 서로 다른 속도로 이동하는 두 포인터 사용
  - 중간 노드 찾기: fast가 끝에 도달 => slow가 중간에 위치
  - 사이클 탐지
  - n번째 뒤 원소 찾기: 한 포인터를 먼저 n칸 보내고 두 포인터 동시 이동

---

## Q6. 연결 리스트를 뒤집는(reverse) 알고리즘을 설명하면?

[답변]
* 포인터 방향 하나씩 뒤집음
* 세 포인터 사용
* 동작
  - 시작: prev = null, curr = head
  - 반복
    1. next = curr.next (다음 노드 임시 저장)
    2. curr.next = prev (링크 방향 뒤집기)
    3. prev = curr (prev 전진)
    4. curr = next (curr 전진)

---

## 📎 참고 자료
<!-- 공부하면서 참고한 링크를 여기에 추가해주세요 -->

---

## 💬 토론 포인트
<!-- PR 리뷰 또는 스터디 중 나온 추가 질문이나 논의 사항을 기록해주세요 -->
