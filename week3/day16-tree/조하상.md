# Day 16: 트리(Tree) 기본 개념
> 📅 2025.02.28 | 📁 Week 3
---
## 🧠 학습 질문
- [x] 트리에서 사이클이 없어야 하는 이유는?
- [x] 완전 이진 트리를 배열로 표현할 수 있는 이유는?
- [x] 배열로 표현된 트리에서 부모/자식 인덱스를 계산하는 공식은?
- [x] 전위/중위/후위 순회는 각각 언제 사용하는가?
- [x] 레벨 순회(Level-order)는 어떻게 구현하는가?
- [x] 편향 트리(Skewed Tree)의 문제점은?
---

## 1. 트리에서 사이클이 없어야 하는 이유는?

**계층적 구조의 유지**
- 트리는 기본적으로 '방향성이 있는 비순환 그래프(DAG)'의 한 종류다. 만약 사이클이 존재한다면 특정 노드에서 다시 자기 자신으로 돌아오는 경로가 생기며, 이는 부모-자식이라는 명확한 **계층적 관계**를 깨뜨린다.
- 사이클이 생기면 노드 간의 경로가 유일하지 않게 되어 트리의 정의(임의의 두 노드 사이의 경로는 유일하다)에 어긋나게 된다.



---

## 2. 완전 이진 트리를 배열로 표현할 수 있는 이유는?

**빈틈없는 노드 배치**
- 완전 이진 트리는 마지막 레벨을 제외하고 모든 레벨이 꽉 차 있으며, 마지막 레벨마저 왼쪽부터 차례대로 채워진 트리다.
- 이러한 구조적 특징 덕분에 각 노드에 번호를 매겼을 때 중간에 비는 인덱스 없이 **연속적인 배열 요소**로 매핑이 가능하다. 메모리 낭비가 거의 없고, 포인터를 사용하지 않아 공간 효율성도 높다.



---

## 3. 배열 기반 트리에서의 부모/자식 인덱스 계산 공식

현재 노드의 인덱스를 $i$라고 할 때(루트 노드가 인덱스 0인 경우):
- **왼쪽 자식 노드 인덱스:** $2i + 1$
- **오른쪽 자식 노드 인덱스:** $2i + 2$
- **부모 노드 인덱스:** $(i - 1) // 2$ (단, 결과값의 소수점은 버린다)

*참고: 루트 인덱스를 1로 설정할 경우, 왼쪽 자식은 $2i$, 오른쪽 자식은 $2i + 1$, 부모는 $i // 2$가 되어 계산이 더 직관적이다.*

---

## 4. 전위/중위/후위 순회의 사용 사례

- **전위 순회(Pre-order):** 루트를 먼저 방문하므로 **트리를 복사**하거나, 수식의 전위 표기법(Prefix)을 만들 때 사용한다.
- **중위 순회(In-order):** **이진 탐색 트리(BST)**에서 중위 순회를 하면 노드 값들을 오름차순으로 정렬된 상태로 얻을 수 있다.
- **후위 순회(Post-order):** 자식 노드를 먼저 처리해야 하는 **트리 삭제**, 혹은 폴더의 용량 계산(하위 디렉토리 크기 합산)이나 수식의 후위 표기법(Postfix)에 사용한다.



---

## 5. 레벨 순회(Level-order)의 구현 방식

**큐(Queue)를 이용한 BFS**
- 루트 노드부터 시작하여 인접한 레벨의 노드들을 차례대로 방문하는 방식이다.
- **동작 방식:**
  1. 큐에 루트 노드를 넣는다.
  2. 큐에서 노드를 꺼내 방문 처리하고, 해당 노드의 왼쪽/오른쪽 자식이 있다면 큐에 순서대로 넣는다.
  3. 큐가 빌 때까지 위 과정을 반복한다.

---

## 6. 편향 트리(Skewed Tree)의 문제점

**이진 탐색 성능의 상실**
- 노드들이 한쪽 방향(왼쪽 또는 오른쪽)으로만 치우쳐져 연결 리스트와 같은 선형 구조가 된 상태를 말한다.
- **문제점:** 트리의 높이가 $N$에 비례하게 되어, 본래 트리의 장점인 탐색/삽입/삭제의 시간 복잡도 $O(\log N)$이 무너지고 **최악의 경우 $O(N)$**이 걸리게 된다. 이를 방지하기 위해 AVL 트리나 Red-Black 트리 같은 균형 이진 탐색 트리를 사용한다.
  

## 📎 참고 자료
<!-- 공부하면서 참고한 링크를 여기에 추가해주세요 -->
---
## 💬 토론 포인트
<!-- PR 리뷰 또는 스터디 중 나온 추가 질문이나 논의 사항을 기록해주세요 -->
