# Day 15: HTTP와 HTTPS
> 📅 2025.02.27 | 📁 Week 3
---
## 🧠 학습 질문
- [x] HTTP는 어떻게 동작하는가? Stateless의 의미는?
- [x] HTTP 메서드(GET, POST, PUT, PATCH, DELETE)의 차이와 멱등성은?
- [x] HTTP 상태 코드의 의미는? (2xx, 3xx, 4xx, 5xx) 주요 코드는?
- [x] HTTP/1.1과 HTTP/2의 차이는? HTTP/2가 더 빠른 이유는?
- [x] HTTP/3는 무엇이 다른가? QUIC 프로토콜이란?
- [x] HTTPS는 어떻게 보안을 제공하는가? 대칭키와 비대칭키의 역할은?
- [x] SSL/TLS 핸드셰이크는 어떻게 동작하는가?
---
## 1. HTTP 동작 원리와 Stateless의 의미

**동작 방식**
- 클라이언트가 서버에 **요청(Request)**을 보내면, 서버가 그에 대한 **응답(Response)**을 주는 방식으로 동작한다. 기본적으로 TCP/IP 프로토콜 위에서 실행된다.

**Stateless (무상태성)**
- 서버가 클라이언트의 이전 상태(이전 요청 내용 등)를 보존하지 않는다는 것을 의미한다.
- **장점:** 서버가 클라이언트의 상태를 관리할 필요가 없어 서버 확장이 용이하다(Scalability).
- **보완:** 로그인을 유지하는 것과 같은 상태 유지가 필요한 기능은 쿠키(Cookie), 세션(Session), 토큰(JWT) 등을 사용해 별도로 처리한다.

---
## 2. HTTP 메서드의 차이와 멱등성(Idempotency)
**주요 메서드 역할**
- **GET:** 서버의 리소스를 조회한다.
- **POST:** 새로운 리소스를 생성하거나 데이터를 처리한다.
- **PUT:** 리소스를 전체적으로 교체(Update)한다.
- **PATCH:** 리소스를 부분적으로 수정한다.
- **DELETE:** 리소스를 삭제한다.

**멱등성 (Idempotency)**
- 연산을 여러 번 수행해도 결과가 동일한 성질을 의미한다.
- **GET, PUT, DELETE**는 멱등하다. (여러 번 요청해도 리소스의 상태가 같음)
- **POST, PATCH**는 멱등하지 않다. (POST는 매번 새로운 리소스를 생성할 수 있고, PATCH는 구현 방식에 따라 결과가 달라질 수 있음)

---
## 3. HTTP 상태 코드의 의미와 주요 코드
- **2xx (Success):** 요청이 성공적으로 처리됨. (예: 200 OK, 201 Created)
- **3xx (Redirection):** 요청 완료를 위해 추가 동작이 필요함. (예: 301 Moved Permanently, 302 Found)
- **4xx (Client Error):** 클라이언트의 요청에 오류가 있음. (예: 400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found)
- **5xx (Server Error):** 서버가 요청을 처리하는 중에 오류가 발생함. (예: 500 Internal Server Error, 503 Service Unavailable)

---
## 4. HTTP/1.1과 HTTP/2의 차이
**HTTP/2가 더 빠른 이유**
- **Binary Framing:** 텍스트 대신 바이너리 형식으로 데이터를 전달하여 파싱 속도가 빠르고 오류 발생 가능성이 낮다.
- **Multiplexing:** 하나의 커넥션에서 여러 요청과 응답을 동시에 주고받을 수 있어 HTTP/1.1의 HOL(Head-of-Line) Blocking 문제를 해결한다.
- **Header Compression (HPACK):** 중복되는 헤더 정보를 압축하여 전송 데이터 크기를 줄인다.
- **Server Push:** 클라이언트가 요청하지 않아도 서버가 필요한 리소스를 미리 보내줄 수 있다.



---

## 5. HTTP/3와 QUIC 프로토콜의 특징
**UDP 기반의 발전**
- **QUIC (Quick UDP Internet Connections):** TCP 대신 **UDP**를 기반으로 설계된 프로토콜이다.
- **연결 설정 단축:** TCP의 3-way handshake 과정을 생략하거나 단축하여 연결 지연 시간(Latency)을 줄인다.
- **패킷 손실 대응:** TCP는 패킷 하나가 손실되면 전체 전송이 멈추지만, QUIC은 독립적인 스트림 구조를 가져 다른 스트림에 영향을 주지 않는다. 이를 통해 네트워크 환경이 불안정한 이동통신에서도 성능을 유지한다.



---
## 6. HTTPS의 보안 메커니즘 (대칭키와 비대칭키)

HTTPS는 HTTP에 **SSL/TLS** 암호화를 추가한 프로토콜이다.
- **비대칭키 (Asymmetric Key):** 암호화와 복호화에 사용하는 키가 서로 다르다(공개키/개인키). 처음 연결 시 안전하게 세션 키를 공유하기 위해 사용한다.
- **대칭키 (Symmetric Key):** 암호화와 복호화에 같은 키를 사용한다. 실제 데이터를 주고받을 때는 속도가 빠른 대칭키를 사용하여 효율성을 높인다.
- 즉, **비대칭키를 이용해 대칭키를 안전하게 교환**한 뒤, 이후 통신은 대칭키로 진행하는 하이브리드 방식을 취한다.

---
## 7. SSL/TLS 핸드셰이크 동작 과정

데이터 통신을 시작하기 전, 신뢰할 수 있는 연결인지 확인하는 과정이다.

1. **Client Hello:** 클라이언트가 서버에 지원 가능한 암호 방식 등을 보낸다.
2. **Server Hello & Certificate:** 서버가 암호 방식 하나를 선택하고 자신의 인증서(공개키 포함)를 보낸다.
3. **Key Exchange:** 클라이언트는 인증서를 검증한 후, 대칭키로 사용할 **Pre-master Secret**을 생성하여 서버의 공개키로 암호화해 보낸다.
4. **Change Cipher Spec:** 서버와 클라이언트 모두 전달받은 정보를 바탕으로 동일한 **세션 키(대칭키)**를 생성하고 암호화 통신 준비가 완료되었음을 알린다.

---
## 📎 참고 자료
<!-- 공부하면서 참고한 링크를 여기에 추가해주세요 -->
---
## 💬 토론 포인트
<!-- PR 리뷰 또는 스터디 중 나온 추가 질문이나 논의 사항을 기록해주세요 -->
