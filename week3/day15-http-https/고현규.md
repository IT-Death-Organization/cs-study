# Day 15: HTTP와 HTTPS
> 📅 2025.02.27 | 📁 Week 3
---
## 🧠 학습 질문
- [x] HTTP는 어떻게 동작하는가? Stateless의 의미는?
- [x] HTTP 메서드(GET, POST, PUT, PATCH, DELETE)의 차이와 멱등성은?
- [x] HTTP 상태 코드의 의미는? (2xx, 3xx, 4xx, 5xx) 주요 코드는?
- [x] HTTP/1.1과 HTTP/2의 차이는? HTTP/2가 더 빠른 이유는?
- [x] HTTP/3는 무엇이 다른가? QUIC 프로토콜이란?
- [x] HTTPS는 어떻게 보안을 제공하는가? 대칭키와 비대칭키의 역할은?
- [x] SSL/TLS 핸드셰이크는 어떻게 동작하는가?
---

# 1. HTTP는 어떻게 동작하는가? Stateless의 의미는?

> HTTP(HyperText Transfer Protocol)는 클라이언트와 서버가 데이터를 주고받기 위한 요청-응답(Request-Response) 방식의 프로토콜입니다.

### Stateless (무상태성)

HTTP의 가장 큰 특징 중 하나는 서버가 클라이언트의 이전 상태를 보존하지 않는다는 것입니다.

* 의미
    * 각 요청은 독립적입니다. 서버는 방금 전의 요청이 누구에게서 온 것인지, 이전에 무엇을 했는지 기억하지 못합니다.
* 장점
    * 서버가 클라이언트 정보를 유지할 필요가 없어 서버 확장성(Scalability)이 좋습니다.
* 단점
   * 로그인이 유지되어야 하는 서비스에서는 불편함이 생깁니다. 이를 해결하기 위해 브라우저의 쿠키(Cookie)나 서버의 세션(Session), JWT(Json Web Token) 등을 활용해 상태를 유지합니다.

초창기 HTTP는 응답 후 바로 연결을 끊는 비연결성(Connectionless)을 지향했습니다. 하지만 현대 HTTP는 성능을 위해 연결은 유지하되, 논리적인 상태는 기억하지 않는 Stateless 방식을 사용합니다.

* Stateless: 기억하지 않음
* Connectionless: 연결을 유지하지 않음

<br>

# 2. HTTP 메서드(GET, POST, PUT, PATCH, DELETE)의 차이와 멱등성은?

> HTTP 메서드는 클라이언트가 서버에게 요청의 목적(의도)을 알리는 방법입니다.

### 메서드별 특징과 멱등성 (Idempotency)
멱등성이란 연산을 여러 번 수행해도 결과가 달라지지 않는 성질을 말합니다.

| 메서드 | 역할 | 멱등성 | 특징 |
| :--- | :--- | :---: | :--- |
| **GET** | 리소스 조회 | **O** | 데이터를 읽기만 하므로 여러 번 조회해도 데이터가 변하지 않음. |
| **POST** | 리소스 생성 | **X** | 수행할 때마다 새로운 데이터가 생성됨. (결과가 계속 변함) |
| **PUT** | 리소스 대체 | **O** | 요청한 데이터로 통째로 덮어씀. 여러 번 수행해도 결과는 동일함. |
| **PATCH** | 리소스 일부 수정 | **X** | 부분 수정이므로 구현 방식에 따라 결과가 달라질 수 있음. (비멱등으로 설계 가능) |
| **DELETE** | 리소스 삭제 | **O** | 한 번 삭제하나 열 번 삭제하나 해당 데이터가 없는 결과는 동일함. |

<br>

# 3. HTTP 상태 코드의 의미는? (2xx, 3xx, 4xx, 5xx) 주요 코드는?

> 서버가 클라이언트의 요청에 대한 처리 결과를 숫자로 응답하는 방식입니다.

### 2xx (Success)
요청을 성공적으로 처리함.
* `200 OK`: 요청 성공.
* `201 Created`: 새로운 리소스 생성 성공 (POST 요청 후 주로 사용).

### 3xx (Redirection)
요청 완료를 위해 추가 동작이 필요함.
* `301 Moved Permanently`: 리소스 주소가 영구적으로 이동됨.
* `302 Found`: 일시적인 주소 이동.

### 4xx (Client Error)
클라이언트의 요청이 잘못됨.
* `400 Bad Request`: 요청 구문이 잘못됨.
* `401 Unauthorized`: 인증되지 않은 사용자.
* `403 Forbidden`: 신원은 확인됐지만 접근이 거부됨.
* `404 Not Found`: 리소스를 찾을 수 없음.

### 5xx (Server Error)
서버 내부 문제로 처리에 실패함.
* `500 Internal Server Error`: 서버 내부 에러 (범용).
* `503 Service Unavailable`: 서버 과부하 등으로 일시적 서비스 중단.

<br>

# 4. HTTP/1.1과 HTTP/2의 차이는? HTTP/2가 더 빠른 이유는?

### HTTP/1.1의 한계
HTTP/1.1은 기본적으로 한 번에 하나의 요청만 처리할 수 있습니다. (Head-of-Line Blocking)
* 지연 시간 증가
    * 이미지를 하나 다운로드 받으려면 요청 -> 응답 -> 요청 -> 응답... 순차적으로 처리해야 해서 시간이 오래 걸립니다. 즉, 앞의 요청이 지연되면 뒤의 요청들은 무조건 대기 해야합니다.
* 연결 오버헤드
    * 요청마다 TCP 연결을 맺고 끊는 과정에서 오버헤드가 발생합니다.

### HTTP/2의 혁신
HTTP/2는 이러한 한계를 극복하기 위해 메시지 전성 방식 자체를 텍스트에서 바이너리로 바꿨습니다. (Binary Framing Layer)

* 프레임(Frame)
    * 메시지를 더 작은 단위인 프레임으로 쪼개고 바이너리로 인코딩 합니다.
    * 파싱 속도가 비약적으로 향상되었고, 하나의 연결 내에서 여러 프레임을 섞어서 보낼 수 있는 멀티플렉싱의 기반이 됩니다.

### 핵심 기술

1. 멀티플렉싱 (Multiplexing)
   * 하나의 TCP 연결 내에서 여러 개의 스트림(Stream)을 통해 응답 순서에 상관없이 데이터를 동시에 주고받을 수 있습니다. (HOLB 문제 해결)
   * 마치 고속도로의 여러 차선처럼, 데이터가 섞여서 가지만 도착지에서 분리됩니다.

2. 헤더 압축 (Header Compression)
   * HTTP/1.1에서는 매 요청마다 헤더를 반복해서 보냈지만, HTTP/2는 HPACK 압축을 통해 중복 데이터를 제거하여 전송량을 줄입니다.
   * Static Table과 Dynamic Table을 사용하여 중복되는 헤더 정보는 인덱스 번호만 전송합니다. 헤더 크기를 약 80% 이상 줄여 대역폭 효율을 극대화했습니다.

3. 서버 푸시 (Server Push)
   * 클라이언트가 요청하기 전에 서버가 필요한 리소스(CSS, JS 등)를 미리 보내주는 기능입니다. 이를 통해 클라이언트의 추가 요청에 따른 RTT(Round Trip Time)를 절약합니다.

<br>

# 5. HTTP/3는 무엇이 다른가? QUIC 프로토콜이란?

> HTTP/2가 TCP 위에서 동작했다면, HTTP/3는 **UDP** 기반의 **QUIC** 프로토콜 위에서 동작합니다.

* TCP의 한계 극복
    * TCP는 연결 설정 과정(3-Way Handshake)이 길고, 패킷 하나만 유실되어도 전체가 멈추는 한계가 있었습니다.
* QUIC의 특징
    * UDP를 사용하여 연결 설정 시간을 단축하고, 각 스트림을 독립적으로 처리하여 패킷 유실 시에도 해당 데이터만 재전송하면 됩니다
    * 이동 중 IP가 바뀌어도 연결을 끊지 않고 유지할 수 있다는 장점이 있습니다.

<br>

# 6. HTTPS는 어떻게 보안을 제공하는가? 대칭키와 비대칭키의 역할은?

> HTTPS는 HTTP에 SSL/TLS 보안 계층을 추가한 프로토콜입니다.

### 대칭키와 비대칭키
HTTPS는 성능과 보안을 모두 잡기 위해 두 방식을 혼합해서 사용합니다.

1. 비대칭키 (공개키/개인키)
   * 보안성은 높지만 연산이 매우 무겁습니다. 그래서 처음 연결할 때만 서로를 신뢰하고 '대칭키'를 안전하게 전달하는 용도로 씁니다.
2. 대칭키
   * 보안성은 비대칭키보다 낮지만 연산이 매우 빠릅니다. 실제 데이터를 주고받을 때는 이 대칭키를 사용하여 암호화합니다.

<br>

# 7. SSL/TLS 핸드셰이크는 어떻게 동작하는가?

> 데이터를 주고받기 전, 서로를 확인하고 대칭키를 생성하는 과정입니다.

1. Client Hello
    * 클라이언트가 서버에게 지원하는 암호화 방식 등을 보냅니다.
2. Server Hello & Certificate
    * 서버가 암호화 방식 하나를 고르고, 본인의 인증서(Certificate)를 보냅니다.
3. 인증서 검증
    * 클라이언트는 브라우저에 내장된 CA(인증 기관) 리스트를 통해 서버의 인증서가 진짜인지 확인합니다.
4. Pre-master Secret 전달
    * 서버의 공개키를 이용해 비밀 키 조각을 암호화하여 서버에 보냅니다.
5. 대칭키 생성
    * 서버와 클라이언트는 전달받은 조각들로 동일한 세션 키(대칭키)를 만듭니다.
6. Finished
    * 이제 이 대칭키로 암호화된 통신을 시작합니다.