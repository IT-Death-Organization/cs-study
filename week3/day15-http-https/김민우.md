# Day 15: HTTP와 HTTPS
> 📅 2025.02.27 | 📁 Week 3
---
## 🧠 학습 질문
- [x] HTTP는 어떻게 동작하는가? Stateless의 의미는?
- [x] HTTP 메서드(GET, POST, PUT, PATCH, DELETE)의 차이와 멱등성은?
- [x] HTTP 상태 코드의 의미는? (2xx, 3xx, 4xx, 5xx) 주요 코드는?
- [x] HTTP/1.1과 HTTP/2의 차이는? HTTP/2가 더 빠른 이유는?
- [x] HTTP/3는 무엇이 다른가? QUIC 프로토콜이란?
- [x] HTTPS는 어떻게 보안을 제공하는가? 대칭키와 비대칭키의 역할은?
- [x] SSL/TLS 핸드셰이크는 어떻게 동작하는가?
---
아래는 **네트워크/백엔드 면접·시험에서 자주 묻는 HTTP 핵심을 “정확한 용어 + 구조” 기준으로 정리한 답변**입니다.

---

## 1️⃣ HTTP는 어떻게 동작하는가? Stateless의 의미

### HTTP 동작 방식

* **클라이언트–서버 모델**
* **요청(Request) → 응답(Response)** 구조
* 기본 흐름

  1. 클라이언트가 서버에 HTTP 요청 전송
  2. 서버가 요청 처리
  3. HTTP 응답 반환

### Stateless(무상태성)의 의미

* **서버가 클라이언트의 이전 요청 상태를 기억하지 않음**
* 각 요청은 **완전히 독립적**

**장점**

* 서버 확장성 ↑ (로드밸런싱, 스케일아웃 용이)
* 장애 복구 쉬움

**단점**

* 로그인 상태 유지 불가 → **Cookie / Session / JWT** 필요

---

## 2️⃣ HTTP 메서드 차이 & 멱등성(Idempotency)

### 멱등성 개념

* **같은 요청을 여러 번 보내도 서버 상태가 동일하면 멱등**
* 네트워크 재시도, 장애 복구에서 중요

| 메서드    | 의미       | 서버 상태 변경 | 멱등성    |
| ------ | -------- | -------- | ------ |
| GET    | 조회       | ❌        | ✅      |
| POST   | 생성       | ✅        | ❌      |
| PUT    | 전체 수정/대체 | ✅        | ✅      |
| PATCH  | 부분 수정    | ✅        | ❌(일반적) |
| DELETE | 삭제       | ✅        | ✅      |

**핵심 포인트**

* `PUT`은 **자원의 전체를 동일한 값으로 덮어씀 → 멱등**
* `POST`는 **요청마다 새로운 리소스 생성 가능 → 비멱등**

---

## 3️⃣ HTTP 상태 코드 의미

### 범위별 의미

* **2xx**: 성공
* **3xx**: 리다이렉션
* **4xx**: 클라이언트 오류
* **5xx**: 서버 오류

### 주요 상태 코드

#### 2xx

* **200 OK**: 요청 성공
* **201 Created**: 리소스 생성 성공
* **204 No Content**: 성공했지만 응답 본문 없음

#### 3xx

* **301 Moved Permanently**: 영구 이동
* **302 Found**: 임시 이동
* **304 Not Modified**: 캐시 사용

#### 4xx

* **400 Bad Request**: 요청 문법 오류
* **401 Unauthorized**: 인증 필요
* **403 Forbidden**: 권한 없음
* **404 Not Found**: 리소스 없음
* **409 Conflict**: 리소스 충돌

#### 5xx

* **500 Internal Server Error**: 서버 내부 오류
* **502 Bad Gateway**: 중간 서버 오류
* **503 Service Unavailable**: 서버 과부하/점검

---

## 4️⃣ HTTP/1.1 vs HTTP/2 차이

### HTTP/1.1 문제점

* **Head-of-Line Blocking**
* 요청/응답 **직렬 처리**
* 다수 TCP 연결 필요

### HTTP/2 개선점

* **단일 TCP 연결**
* **Multiplexing**: 여러 요청/응답 동시 처리
* **Header Compression (HPACK)**
* **Server Push**

### 왜 HTTP/2가 더 빠른가?

* TCP 연결 수 감소
* HOL Blocking 감소
* 헤더 크기 감소
* 네트워크 자원 효율 ↑

---

## 5️⃣ HTTP/3 & QUIC 프로토콜

### HTTP/3의 핵심

* **TCP ❌ → UDP 기반**
* **QUIC 프로토콜 위에서 동작**

### QUIC이란?

* Google이 개발 → IETF 표준화
* **전송 계층 + 암호화 + 흐름 제어 통합**

### HTTP/3 장점

* **Connection-level HOL Blocking 제거**
* 패킷 손실 시 **해당 스트림만 영향**
* **빠른 핸드셰이크 (0-RTT)**

---

## 6️⃣ HTTPS는 어떻게 보안을 제공하는가?

### HTTPS = HTTP + TLS

보안 요소

* **기밀성**: 데이터 암호화
* **무결성**: 변조 방지
* **인증**: 서버 신원 검증

### 대칭키 vs 비대칭키 역할

| 구분 | 대칭키     | 비대칭키       |
| -- | ------- | ---------- |
| 속도 | 빠름      | 느림         |
| 용도 | 데이터 암호화 | 키 교환, 인증   |
| 예시 | AES     | RSA, ECDHE |

👉 **실제 데이터는 대칭키로 암호화**,
👉 **대칭키를 안전하게 교환하기 위해 비대칭키 사용**

---

## 7️⃣ SSL/TLS 핸드셰이크 과정 (간단 요약)

1. **ClientHello**

   * TLS 버전, Cipher Suite 제안
2. **ServerHello**

   * 서버 인증서(공개키) 전달
3. **서버 인증**

   * CA 검증
4. **대칭키 생성**

   * 클라이언트가 대칭키 생성
   * 서버 공개키로 암호화하여 전송
5. **대칭키 공유 완료**
6. **암호화된 통신 시작**

※ 최신 TLS(1.3)는 **ECDHE 기반 키 교환 + 단계 축소**

---

## 📎 참고 자료
<!-- 공부하면서 참고한 링크를 여기에 추가해주세요 -->
---
## 💬 토론 포인트
<!-- PR 리뷰 또는 스터디 중 나온 추가 질문이나 논의 사항을 기록해주세요 -->
